import React from 'react';
import { View, Text } from 'react-native';

const TechBookDetail = ({ name }) => {
  return (
    <View>
      <Text>{name}</Text>
    </View>
  );
};

export default TechBookDetail;
